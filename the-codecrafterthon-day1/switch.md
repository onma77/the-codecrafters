# The switch Statement
Use the switch statement to select one of many code blocks to be executed.
A cleaner way to write multiple if statements when checking one variable against many values.

# The Multi-case switch Statement
No break needed Go automatically stops after a case is finished. You don't need to manually type break.

The default case: This acts as the "catch-all" if none of the specific cases match.

# 